# Skin-disease-classification
This program uses a CNN to classify skin diseases from skin lesion images. It applies preprocessing techniques, data augmentation, and trains the model using Keras. The program evaluates the model on the test set and plots the training and validation accuracy.
